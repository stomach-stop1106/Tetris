class HoldView{}

/*
NextView
 ├─ constructor(x, y, size)
 ├─ drawFrame()
 └─ drawBlock(polyomino)

HoldView
 ├─ constructor(x, y, size)
 ├─ drawFrame()
 └─ drawBlock(polyomino)

ScoreView
 ├─ constructor(x, y)
 ├─ drawFrame()
 └─ drawScore(score)

*/